name: dangi_app
description: 北極星 談議アプリ（Windows + iOS）
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

  # ローカルパッケージ依存
  shared_core:
    path: ../../packages/shared_core
  ui_tokens:
    path: ../../packages/ui_tokens
  motion_tokens:
    path: ../../packages/motion_tokens
  l10n_ja:
    path: ../../packages/l10n_ja

  # M2-E3: 永続化
  path_provider: ^2.1.1

  # M3-F1: Auth（永続化）
  shared_preferences: ^2.3.5
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
