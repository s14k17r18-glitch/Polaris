name: shared_core
description: 状態遷移・会話エンジン・データモデル・同期クライアント
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  # M0: 純粋 Dart パッケージ（Flutter 依存なし）
  # M1以降で必要に応じて追加

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
